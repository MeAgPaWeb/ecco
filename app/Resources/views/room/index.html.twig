{% extends 'table.html.twig' %}

{% set page =
    {
        "title": "Salas",
        "subtitle": "Listado",
    }
%}

{% block breadcrumb %}
    <li><a href="#"><i class="fa fa-dashboard"></i> Salas</a></li>
    <li class="active">Listado</li>
{% endblock %}

{% set page =
    {
        "title": "Sala",
        "subtitle": "Listado"
    }
%}

{% set new =
    {
        "route": "room_new",
        "title": "Nueva Sala"
    }
%}

{% block thead %}
<tr>
    <th>Ancho</th>
    <th>Alto</th>
    <th>Largo</th>
    <th>Sup. Vidriada</th>
    <th></th>
</tr>
{% endblock %}
{% block tfoot %}
<tr>
    <th>Ancho</th>
    <th>Alto</th>
    <th>Largo</th>
    <th>Sup. Vidriada</th>
    <th></th>
</tr>
{% endblock %}
{% block tbody %}
  {% for room in rooms %}
      <tr>
          <td>{{ room.width }}</td>
          <td>{{ room.high }}</td>
          <td>{{ room.length }}</td>
          <td>{{ room.glazedSurface }}</td>
          <td class="table-operations">
            <!-- <a href="{{ path('room_show', { 'id': room.id }) }}" title="Ver detalle"><span class="glyphicon glyphicon-zoom-in" aria-hidden="true"></span></a>&nbsp;&nbsp; -->
            <a href="{{ path('room_edit', { 'id': room.id }) }}" title="Editar"><span class="glyphicon glyphicon-edit" aria-hidden="true"></span></a>
          </td>
      </tr>
  {% endfor %}
{% endblock %}
